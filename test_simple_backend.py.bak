#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Test simple pour diagnostiquer les backends CHNeoWave
"""

import sys
import os
import time
from pathlib import Path

# Ajouter le répertoire src au path
src_path = Path(__file__).parent / "src"
sys.path.insert(0, str(src_path))

from PyQt5.QtWidgets import QApplication
from PyQt5.QtCore import QTimer, QEventLoop

from hrneowave.gui.controllers.acquisition_controller import (
    AcquisitionController, AcquisitionConfig, AcquisitionMode
)

def test_simulate_backend():
    """Test simple du backend de simulation"""
    print("=== Test Backend Simulation ===")
    
    # Créer QApplication
    app = QApplication.instance()
    if app is None:
        app = QApplication([])
    
    # Configuration simple
    config = AcquisitionConfig(
        mode=AcquisitionMode.SIMULATE,
        sample_rate=32.0,
        n_channels=4,
        buffer_size=1000
    )
    
    print(f"Configuration: {config.sample_rate} Hz, {config.n_channels} canaux")
    
    # Créer le contrôleur
    controller = AcquisitionController(config)
    print("✓ Contrôleur créé")
    
    # Variables de test
    samples_count = 0
    
    def on_data(data, timestamp):
        nonlocal samples_count
        samples_count += 1
        if samples_count <= 5:  # Afficher les 5 premiers
            print(f"  Échantillon {samples_count}: {data} à {timestamp:.3f}s")
        elif samples_count == 6:
            print("  ...")
    
    def on_error(msg):
        print(f"  ❌ Erreur: {msg}")
    
    # Connecter les signaux
    controller.data_ready.connect(on_data)
    controller.error_occurred.connect(on_error)
    
    # Test de connexion
    print("\n1. Test de connexion...")
    if controller.connect():
        print("✓ Connexion réussie")
    else:
        print("❌ Échec de connexion")
        return False
    
    # Test d'acquisition
    print("\n2. Démarrage de l'acquisition...")
    if controller.start():
        print("✓ Acquisition démarrée")
    else:
        print("❌ Échec de démarrage")
        return False
    
    # Attendre 3 secondes avec traitement des événements
    print("\n3. Acquisition en cours (3s)...")
    
    start_time = time.time()
    while time.time() - start_time < 3.0:
        app.processEvents()
        time.sleep(0.01)  # 10ms
    
    # Arrêter
    print("\n4. Arrêt...")
    controller.stop()
    controller.disconnect()
    
    # Résultats
    expected = int(3.0 * config.sample_rate)
    print(f"\n=== Résultats ===")
    print(f"Échantillons reçus: {samples_count}")
    print(f"Échantillons attendus: ~{expected}")
    
    success = samples_count > 0
    if success:
        print("✅ Test RÉUSSI")
    else:
        print("❌ Test ÉCHOUÉ")
    
    return success

def test_backend_detection():
    """Test de détection des backends"""
    print("\n=== Test Détection Backends ===")
    
    # Test NI-DAQmx
    try:
        from hrneowave.hw.ni_daqmx_backend import NIDAQmxBackend
        print("✓ Backend NI-DAQmx détecté")
        ni_available = True
    except ImportError as e:
        print(f"❌ Backend NI-DAQmx non disponible: {e}")
        ni_available = False
    
    # Test IOtech
    try:
        from hrneowave.hw.iotech_backend import IOtechBackend
        print("✓ Backend IOtech détecté")
        iotech_available = True
    except ImportError as e:
        print(f"❌ Backend IOtech non disponible: {e}")
        iotech_available = False
    
    return ni_available, iotech_available

def main():
    print("CHNeoWave - Test Simple des Backends")
    print("=" * 40)
    
    # Test de détection
    ni_ok, iotech_ok = test_backend_detection()
    
    # Test du backend de simulation
    sim_ok = test_simulate_backend()
    
    print("\n" + "=" * 40)
    print("RÉSUMÉ")
    print("=" * 40)
    print(f"Simulation    : {'✅' if sim_ok else '❌'}")
    print(f"NI-DAQmx      : {'✅' if ni_ok else '❌'}")
    print(f"IOtech        : {'✅' if iotech_ok else '❌'}")
    
    if sim_ok:
        print("\n🎉 Backend de simulation fonctionnel !")
        return 0
    else:
        print("\n💥 Problème avec le backend de simulation")
        return 1

if __name__ == '__main__':
    sys.exit(main())